{
  "kind": "collectionType",
  "collectionName": "locations",
  "info": {
    "name": "location",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": false
  },
  "attributes": {
    "name": {
      "type": "string"
    },
    "sequences": {
      "via": "location",
      "collection": "sequence"
    },
    "creditsPosition": {
      "type": "enumeration",
      "enum": [
        "opening",
        "closing"
      ],
      "required": false
    }
  }
}
