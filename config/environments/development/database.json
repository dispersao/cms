{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-bookshelf",
      "settings": {
        "client": "mysql",
        "database": "dispersao_sequences",
        "host": "${process.env.database_host || '127.0.0.1'}",
        "port": 3306,
        "username": "dispersao_user",
        "password": "${process.env.mysql_dispersao_password}"
      },
      "options": {}
    }
  }
}
